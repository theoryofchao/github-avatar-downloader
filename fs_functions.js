var fs = require(`fs`);
var request = require(`request`);

var saveAvatarImage = function(url, filePath, callback) {
  console.log(url);
  console.log(filePath);
        
};

module.exports = saveAvatarImage;
